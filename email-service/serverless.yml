service: hms-email-service

provider:
  name: aws
  runtime: python3.9
  region: us-east-1

plugins:
  - serverless-offline

functions:
  sendEmail:
    handler: handler.send_email
    events:
      - http:
          path: email
          method: post
          cors: true
